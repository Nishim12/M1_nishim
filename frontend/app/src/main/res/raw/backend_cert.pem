PLACEHOLDER - replace this whole file with the backend's self-signed cert.pem
(the public certificate only, never the private key) once generated on the VM.
See VM deployment notes for the openssl command that creates it.
